# MeetPoint DB 스키마 및 SQL 문서

## 1\. 문서 목적

이 문서는 MeetPoint의 후속 PostgreSQL 및 Supabase 기준 DB 스키마와 SQL 초안을 고정하기 위한 문서이다.

현재 Team A 소스는 friends, chat, 저장 위치, 추천 결과를 로컬 상태와 브라우저 SDK 중심으로 우선 구현하고 있으므로, 본 문서는 이후 DB 연동이 시작될 때 사용할 DDL과 조회, 저장, 검증용 SQL 초안을 정리하는 것을 목표로 한다.

---

## 2\. 범위

현재 Team A 소스에는 Supabase 접근 코드와 repository 구현이 아직 없다. 본 문서의 기본 범위는 후속 DB 연동 단계에서 사용할 다음 4개 테이블의 생성과 조회 기준이다.

1.  users
2.  friends
3.  messages
4.  departure\_locations

다음 항목은 후속 확장 범위로만 다룬다.

1.  recommendations 테이블 생성
2.  추천 결과 이력 저장
3.  WebSocket 기반 실시간 메시지 구조
4.  비밀번호 재설정용 토큰 테이블

### 2.1 현재 구현 대응 관계

현재 UI 소스와 본 문서의 대응 관계는 다음과 같이 이해한다.

1.  users 는 현재 인증 UI와 위치 상태 UI가 후속으로 저장될 대상 구조이다.
2.  friends 는 현재 friends-screen 의 로컬 친구 목록 상태가 후속으로 저장될 대상 구조이다.
3.  messages 는 현재 use-chat-screen-state 의 로컬 메시지 배열이 후속으로 저장될 대상 구조이다.
4.  departure\_locations 는 현재 use-recommendation-flow-state 의 저장 위치 목록 상태가 후속으로 저장될 대상 구조이다.
5.  recommendations 는 현재 추천 결과 카드를 저장하지 않으므로 확장용 참고 구조만 유지한다.

---

## 3\. DB 설계 원칙

1.  후속 DB 엔진은 Supabase PostgreSQL 기준으로 설계한다.
2.  현재 UI 단계에서는 DB 대신 로컬 상태와 fixture 데이터로 동작하고, 후속 쓰기 작업은 Next.js Route Handler를 통해 수행한다.
3.  닉네임 비교와 중복 검사는 nickname이 아니라 nickname\_normalized 기준으로 처리한다.
4.  후속 인증은 DB 세션이 아니라 JWT httpOnly 쿠키 기준으로 처리한다.
5.  후속 DB 연동 1차에서는 users, friends, messages, departure\_locations 4개 테이블을 실제 생성 대상으로 본다.
6.  추천 결과는 요청 시 계산 후 반환하며 DB에는 저장하지 않는다.
7.  users 의 위치 컬럼은 현재 위치 공유값 전용으로 사용하고, 나중에 만나기 모드에서 재사용할 저장 위치는 departure\_locations 에 분리 저장한다.

---

## 4\. 스키마 요약

| 테이블               | 용도                                                  | 후속 DB 연동 시 생성 여부 |
| -------------------- | ----------------------------------------------------- | ------------------------- |
| users                | 사용자 계정, 비밀번호 해시, 마지막 위치 저장          | 생성                      |
| friends              | 사용자 간 친구 관계 저장                              | 생성                      |
| messages             | 댓글형 채팅 메시지 저장                               | 생성                      |
| departure\_locations | 저장된 출발 위치, 최근 사용 위치, 사전 등록 위치 저장 | 생성                      |
| recommendations      | 추천 결과 이력 저장용 확장 테이블                     | 미생성                    |

---

## 5\. 후속 필수 확장 및 생성 순서

아래 SQL과 생성 순서는 현재 저장소에 아직 적용되지 않았으며, Supabase 또는 PostgreSQL 초기 세팅 단계에서 실행할 초안으로 본다.

### 5.1 필수 확장

```
create extension if not exists pgcrypto;
```

설명:

1.  UUID 기본값 생성에 gen\_random\_uuid()를 사용한다.
2.  Supabase PostgreSQL 환경에서 일반적으로 사용할 수 있는 기본 확장 기준이다.

### 5.2 생성 순서

1.  extension 생성
2.  users 생성
3.  friends 생성
4.  messages 생성
5.  departure\_locations 생성
6.  인덱스 생성
7.  선택적으로 recommendations 확장 SQL 보관

---

## 6\. 후속 MVP 필수 DDL

아래 DDL은 현재 UI 소스가 아니라 후속 DB 연동 단계에서 적용할 초안이다. 현재 Team A 소스에는 이를 실행하는 migration 또는 repository 코드가 아직 없다.

### 6.1 users 테이블

```
create table if not exists public.users (
    id uuid primary key default gen_random_uuid(),
    nickname varchar(12) not null,
    nickname_normalized varchar(12) not null,
    password_hash text not null,
    lat double precision null,
    lng double precision null,
    location_updated_at timestamptz null,
    created_at timestamptz not null default now(),
    constraint users_nickname_normalized_key unique (nickname_normalized)
);
```

설계 포인트:

1.  nickname은 화면 표시용 값을 유지한다.
2.  nickname\_normalized는 중복 비교, 검색, 자기 자신 비교에 사용한다.
3.  password\_hash는 평문 저장을 금지하고 해시 값만 저장한다.
4.  lat, lng, location\_updated\_at은 위치 미공유 사용자를 위해 null 허용이다.

### 6.2 friends 테이블

```
create table if not exists public.friends (
    id uuid primary key default gen_random_uuid(),
    user_id uuid not null,
    friend_id uuid not null,
    created_at timestamptz not null default now(),
    constraint friends_user_id_fkey foreign key (user_id) references public.users (id) on delete cascade,
    constraint friends_friend_id_fkey foreign key (friend_id) references public.users (id) on delete cascade,
    constraint friends_user_friend_unique unique (user_id, friend_id),
    constraint friends_not_self check (user_id <> friend_id)
);
```

설계 포인트:

1.  친구 관계는 논리적으로 양방향이므로 A -> B, B -> A 두 row를 함께 유지한다.
2.  unique(user\_id, friend\_id)로 중복 저장을 방지한다.
3.  check(user\_id \<> friend\_id)로 자기 자신 친구 추가를 차단한다.

### 6.3 messages 테이블

```
create table if not exists public.messages (
    id uuid primary key default gen_random_uuid(),
    sender_id uuid not null,
    receiver_id uuid not null,
    content varchar(500) not null,
    created_at timestamptz not null default now(),
    constraint messages_sender_id_fkey foreign key (sender_id) references public.users (id) on delete cascade,
    constraint messages_receiver_id_fkey foreign key (receiver_id) references public.users (id) on delete cascade,
    constraint messages_content_not_blank check (char_length(btrim(content)) >= 1)
);
```

설계 포인트:

1.  댓글형 채팅 MVP 기준으로 본문은 최대 500자 제한을 전제로 둔다.
2.  sender\_id 와 receiver\_id 는 모두 users.id 를 참조한다.
3.  메시지 조회는 created\_at asc, id asc 기준으로 반환한다.

### 6.4 departure\_locations 테이블

```
create table if not exists public.departure_locations (
    id uuid primary key default gen_random_uuid(),
    user_id uuid not null,
    label varchar(100) not null,
    lat double precision not null,
    lng double precision not null,
    location_kind varchar(20) not null default 'recent',
    last_used_at timestamptz not null default now(),
    created_at timestamptz not null default now(),
    updated_at timestamptz not null default now(),
    constraint departure_locations_user_id_fkey foreign key (user_id) references public.users (id) on delete cascade,
    constraint departure_locations_kind_check check (location_kind in ('recent', 'preset')),
    constraint departure_locations_label_not_blank check (char_length(btrim(label)) >= 1)
);
```

설계 포인트:

1.  departure\_locations 는 나중에 만나기 모드에서 재사용할 저장된 출발 위치를 관리한다.
2.  location\_kind 는 recent 또는 preset 으로 구분해 최근 저장 위치와 사전 등록 위치를 같은 구조 안에서 관리한다.
3.  users.lat, users.lng 는 현재 위치 공유값 용도로 유지하고, 저장된 출발 위치와 혼용하지 않는다.
4.  updated\_at 과 last\_used\_at 은 저장 위치 수정 및 재사용 시점 갱신 기준으로 사용한다.

---

## 7\. 후속 권장 인덱스

```
create index if not exists users_nickname_normalized_idx
    on public.users (nickname_normalized);

create index if not exists users_location_updated_at_idx
    on public.users (location_updated_at);

create index if not exists friends_user_id_idx
    on public.friends (user_id);

create index if not exists friends_friend_id_idx
    on public.friends (friend_id);

create index if not exists messages_sender_receiver_created_at_idx
    on public.messages (sender_id, receiver_id, created_at);

create index if not exists messages_receiver_sender_created_at_idx
    on public.messages (receiver_id, sender_id, created_at);

create index if not exists departure_locations_user_last_used_at_idx
    on public.departure_locations (user_id, last_used_at desc);

create index if not exists departure_locations_user_kind_idx
    on public.departure_locations (user_id, location_kind);
```

설계 포인트:

1.  users.nickname\_normalized 인덱스는 닉네임 검색과 중복 검사를 빠르게 한다.
2.  friends 인덱스는 현재 사용자 친구 목록 조회와 역방향 관계 확인에 사용한다.
3.  messages 양방향 인덱스는 두 사용자 간 대화 조회를 단순화한다.
4.  departure\_locations 인덱스는 사용자별 저장 위치 목록 조회와 최근 사용 위치 정렬에 사용한다.

---

## 8\. 후속 초기 생성 SQL 묶음

실행 편의를 위해 후속 DB 초기 구축 시 아래 SQL 블록을 순서대로 사용할 수 있다.

```
create extension if not exists pgcrypto;

create table if not exists public.users (
    id uuid primary key default gen_random_uuid(),
    nickname varchar(12) not null,
    nickname_normalized varchar(12) not null,
    password_hash text not null,
    lat double precision null,
    lng double precision null,
    location_updated_at timestamptz null,
    created_at timestamptz not null default now(),
    constraint users_nickname_normalized_key unique (nickname_normalized)
);

create table if not exists public.friends (
    id uuid primary key default gen_random_uuid(),
    user_id uuid not null,
    friend_id uuid not null,
    created_at timestamptz not null default now(),
    constraint friends_user_id_fkey foreign key (user_id) references public.users (id) on delete cascade,
    constraint friends_friend_id_fkey foreign key (friend_id) references public.users (id) on delete cascade,
    constraint friends_user_friend_unique unique (user_id, friend_id),
    constraint friends_not_self check (user_id <> friend_id)
);

create table if not exists public.messages (
    id uuid primary key default gen_random_uuid(),
    sender_id uuid not null,
    receiver_id uuid not null,
    content varchar(500) not null,
    created_at timestamptz not null default now(),
    constraint messages_sender_id_fkey foreign key (sender_id) references public.users (id) on delete cascade,
    constraint messages_receiver_id_fkey foreign key (receiver_id) references public.users (id) on delete cascade,
    constraint messages_content_not_blank check (char_length(btrim(content)) >= 1)
);

create table if not exists public.departure_locations (
    id uuid primary key default gen_random_uuid(),
    user_id uuid not null,
    label varchar(100) not null,
    lat double precision not null,
    lng double precision not null,
    location_kind varchar(20) not null default 'recent',
    last_used_at timestamptz not null default now(),
    created_at timestamptz not null default now(),
    updated_at timestamptz not null default now(),
    constraint departure_locations_user_id_fkey foreign key (user_id) references public.users (id) on delete cascade,
    constraint departure_locations_kind_check check (location_kind in ('recent', 'preset')),
    constraint departure_locations_label_not_blank check (char_length(btrim(label)) >= 1)
);

create index if not exists users_nickname_normalized_idx
    on public.users (nickname_normalized);

create index if not exists users_location_updated_at_idx
    on public.users (location_updated_at);

create index if not exists friends_user_id_idx
    on public.friends (user_id);

create index if not exists friends_friend_id_idx
    on public.friends (friend_id);

create index if not exists messages_sender_receiver_created_at_idx
    on public.messages (sender_id, receiver_id, created_at);

create index if not exists messages_receiver_sender_created_at_idx
    on public.messages (receiver_id, sender_id, created_at);

create index if not exists departure_locations_user_last_used_at_idx
    on public.departure_locations (user_id, last_used_at desc);

create index if not exists departure_locations_user_kind_idx
    on public.departure_locations (user_id, location_kind);
```

---

## 9\. 후속 API 연동 기준 SQL

아래 SQL은 05.API 명세서의 후속 Route Handler 계약을 실제 DB 조회와 저장으로 연결할 때 사용할 초안이다. 현재 app/api 와 lib/repositories 는 이 SQL을 호출하는 구현이 아직 없다.

### 9.1 회원가입용 사용자 중복 검사

```
select id, nickname, nickname_normalized
from public.users
where nickname_normalized = $1
limit 1;
```

설명:

1.  $1 에는 정규화된 nickname\_normalized 값이 들어간다.
2.  결과가 있으면 회원가입 시 CONFLICT\_NICKNAME 처리 대상이다.

### 9.2 회원가입용 사용자 생성

```
insert into public.users (
    nickname,
    nickname_normalized,
    password_hash
) values (
    $1,
    $2,
    $3
)
returning id, nickname, lat, lng, location_updated_at as "locationUpdatedAt";
```

### 9.3 로그인용 사용자 조회

```
select id, nickname, nickname_normalized, password_hash, lat, lng, location_updated_at
from public.users
where nickname_normalized = $1
limit 1;
```

설명:

1.  비밀번호 검증은 애플리케이션 레이어에서 password\_hash 비교로 수행한다.
2.  DB는 조회와 저장 기준만 담당한다.

### 9.4 친구 추가 대상 사용자 조회

```
select id, nickname, lat, lng, location_updated_at
from public.users
where nickname_normalized = $1
limit 1;
```

### 9.5 친구 관계 양방향 upsert

```
insert into public.friends (user_id, friend_id)
values
    ($1, $2),
    ($2, $1)
on conflict (user_id, friend_id) do nothing;
```

설명:

1.  $1 은 현재 사용자 ID, $2 는 친구 대상 사용자 ID이다.
2.  친구 추가는 트랜잭션 안에서 처리하는 것을 권장한다.

### 9.6 친구 목록 조회

```
select
    f.id as relation_id,
    u.id,
    u.nickname,
    u.lat,
    u.lng,
    u.location_updated_at
from public.friends f
join public.users u
    on u.id = f.friend_id
where f.user_id = $1
order by u.nickname asc;
```

### 9.7 메시지 저장

```
insert into public.messages (
    sender_id,
    receiver_id,
    content
) values (
    $1,
    $2,
    $3
)
returning id, sender_id, receiver_id, content, created_at;
```

### 9.8 메시지 조회

```
select id, sender_id, receiver_id, content, created_at
from public.messages
where (
        sender_id = $1 and receiver_id = $2
    )
    or (
        sender_id = $2 and receiver_id = $1
    )
order by created_at asc, id asc
limit $3;
```

### 9.9 polling 메시지 조회

```
select id, sender_id, receiver_id, content, created_at
from public.messages
where (
        (sender_id = $1 and receiver_id = $2)
        or (sender_id = $2 and receiver_id = $1)
    )
    and created_at > $3
order by created_at asc, id asc
limit $4;
```

### 9.10 위치 저장

```
update public.users
set
    lat = $2,
    lng = $3,
    location_updated_at = now()
where id = $1
returning lat, lng, location_updated_at;
```

### 9.11 저장된 출발 위치 목록 조회

```
select id, label, lat, lng, location_kind, last_used_at, created_at, updated_at
from public.departure_locations
where user_id = $1
order by
    case when location_kind = 'preset' then 0 else 1 end,
    last_used_at desc,
    created_at desc
limit $2;
```

설명:

1.  $1 은 현재 사용자 ID, $2 는 조회 개수 제한이다.
2.  사전 등록 위치를 먼저 보여주고, 그 다음 최근 저장 위치를 최근 사용 순으로 반환한다.

### 9.12 저장된 출발 위치 추가

```
insert into public.departure_locations (
    user_id,
    label,
    lat,
    lng,
    location_kind
) values (
    $1,
    $2,
    $3,
    $4,
    $5
)
returning id, label, lat, lng, location_kind, last_used_at, created_at, updated_at;
```

설명:

1.  $5 는 recent 또는 preset 값을 사용한다.
2.  저장 위치 생성과 수정 정책은 서비스 레이어에서 UI 정책에 맞게 제한한다.

### 9.13 저장된 출발 위치 최근 사용 시각 갱신

```
update public.departure_locations
set
    last_used_at = now(),
    updated_at = now()
where id = $1
  and user_id = $2
returning id, label, lat, lng, location_kind, last_used_at, updated_at;
```

### 9.14 추천 전 위치 조회

```
select id, nickname, lat, lng, location_updated_at
from public.users
where id in ($1, $2);
```

### 9.15 친구 관계 검증

```
select 1
from public.friends
where user_id = $1
  and friend_id = $2
limit 1;
```

---

## 10\. 후속 트랜잭션 권장 구간

다음 작업은 한 트랜잭션 안에서 처리하는 것을 권장한다.

1.  회원가입 시 사용자 생성과 후속 조회
2.  친구 추가 시 양방향 friends row 생성
3.  친구 추가 시 비정상 단방향 관계 복구

예시:

```
begin;

insert into public.friends (user_id, friend_id)
values
    ($1, $2),
    ($2, $1)
on conflict (user_id, friend_id) do nothing;

commit;
```

---

## 11\. recommendations 확장 SQL 초안

아래 SQL은 현재 단계에서 실행하지 않고, 후속 확장 시 참고용으로만 보관한다.

```
create table if not exists public.recommendations (
    id uuid primary key default gen_random_uuid(),
    user_id uuid not null,
    friend_id uuid not null,
    midpoint_lat double precision not null,
    midpoint_lng double precision not null,
    place_name varchar(200) not null,
    place_lat double precision not null,
    place_lng double precision not null,
    category varchar(100) not null,
    mode varchar(20) not null,
    score double precision not null,
    created_at timestamptz not null default now(),
    constraint recommendations_user_id_fkey foreign key (user_id) references public.users (id) on delete cascade,
    constraint recommendations_friend_id_fkey foreign key (friend_id) references public.users (id) on delete cascade
);

create index if not exists recommendations_user_friend_created_at_idx
    on public.recommendations (user_id, friend_id, created_at desc);
```

주의 사항:

1.  현재 MVP에서는 recommendations 테이블을 생성하지 않는다.
2.  추천 이력 UI와 조회 API가 필요할 때만 도입한다.

### 11.1 recommendations 후속 도입 판단 기준

아래 요구사항이 실제 기능 범위에 포함될 때 recommendations 테이블 도입을 진행한다.

1.  추천 결과 이력 보기
2.  최근 추천 재사용
3.  추천 통계 또는 분석
4.  추천 결과 공유 또는 즐겨찾기
5.  디버깅용 추천 로그 보관

운영 원칙:

1.  현재 MVP 단계에서는 기능 범위를 줄이기 위해 recommendations 테이블을 생성하지 않는다.
2.  추천 결과는 요청 시 계산 후 즉시 반환하는 구조로 유지한다.
3.  실제로 저장이 필요한 기능이 확정되는 순간에만 recommendations 테이블과 관련 API, 조회 화면, 마이그레이션을 함께 추가한다.
4.  즉, 테이블을 미리 만들어 두는 방식보다 필요한 시점에 목적을 분명히 하고 추가하는 방식을 기본 원칙으로 한다.

---

## 12\. 검증 체크리스트

아래 체크리스트는 후속 DB 연동 단계에서 확인할 항목이다. 현재 UI-only 단계에서는 문서 정합성과 데이터 구조 방향만 먼저 맞춘다.

1.  users.nickname\_normalized unique 제약이 정상 동작하는지 확인한다.
2.  friends 에서 자기 자신 추가가 check 제약으로 차단되는지 확인한다.
3.  friends 의 양방향 upsert 가 중복 없이 동작하는지 확인한다.
4.  messages.content 공백-only 입력이 차단되는지 확인한다.
5.  users 위치 갱신 후 location\_updated\_at 이 함께 바뀌는지 확인한다.
6.  메시지 조회가 created\_at asc, id asc 기준으로 안정적으로 정렬되는지 확인한다.
7.  departure\_locations.location\_kind check 제약이 recent, preset 외 값을 차단하는지 확인한다.
8.  저장된 출발 위치 목록 조회가 last\_used\_at 기준으로 정렬되는지 확인한다.
9.  recommendations 테이블이 MVP 초기 SQL에 포함되지 않았는지 확인한다.

---

## 13\. 문서 연계

1.  데이터 구조 개요는 04.ERD.md 를 따른다.
2.  API 요청 및 응답 계약은 05.API 명세서.md 를 따른다.
3.  구현 파일과 Route Handler 연결 기준은 03.기술 설계.md 를 따른다.
4.  MVP 범위 판단은 01.기획.md 와 02.구조 설계.md 를 따른다.
5.  현재 UI-only 구현 범위와 후속 DB 연동 범위 구분은 03.기술 설계.md 와 동일한 기준을 따른다.